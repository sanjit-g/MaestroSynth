# MaestroSynth dependencies
# Install: pip install -r requirements.txt
# Optional (for MIDI on Windows): pip install python-rtmidi  (needs C++ build tools)

opencv-python>=4.8.0
mediapipe>=0.10.0
mido>=1.3.0
Pillow>=9.0.0
